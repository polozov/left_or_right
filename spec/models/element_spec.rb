describe Element do
  # subject{FactoryGirl.build(:element)}

  # it { should validate_presence_of(:name) }
  # it { should validate_uniqueness_of(:name) }
  # it { should validate_presence_of(:image) }
end