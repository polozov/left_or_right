<h3>Выбранная категория: <%= @category.name.capitalize %></h3>

<p>
  <%= @left.name.capitalize %>
  <%= link_to (image_tag @left.path), vote_category_element_path(category_id: @category.id, id: @left.id) %><br />
  Голосов: <%= @left.score %>
</p>
<p>
  <%= @right.name.capitalize %>
  <%= link_to (image_tag @right.path), vote_category_element_path(category_id: @category.id, id: @right.id) %><br />
  Голосов: <%= @right.score %>
</p>
<br />
<p><%= link_to 'К списку категорий', root_path %></p>
<p><%= link_to 'Создать элемент',
      new_category_element_path(category_id: @category.id) %></p>
